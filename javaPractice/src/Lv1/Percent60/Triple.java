package Lv1.Percent60;

// 3명의 정수 번호 합이 0이 되면 삼총사

public class Triple {
    public int solution(int[] number) {
        int answer = 0;
        return answer;
    }

    // 배열에서 하나를 꺼내서 그 중 큰 값을 a, 작은 값을 b
    // 다음 max가 a 보다 크다면 대
    //
    //

    // 결과 테스트
    public static void main(String[] args) {
        Triple sol = new Triple();
        int[] number = {-2, 3, 0, 2, -5};

        System.out.println(sol.solution(number));

    }
}
